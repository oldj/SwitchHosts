[package]
name = "switchhosts-core"
version = "5.0.0"
edition = "2021"
description = "Platform-agnostic SwitchHosts business logic shared by Tauri and WASM."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
